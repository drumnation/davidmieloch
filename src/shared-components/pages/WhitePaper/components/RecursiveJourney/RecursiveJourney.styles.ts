import styled, { useTheme } from 'styled-components';
import { DefaultTheme } from 'styled-components';

// Define common styles or theme helpers if needed
export const RecursiveJourneyStyles = styled.div`
  /* Add global or container styles specific to RecursiveJourney here if necessary */
`;

// Hook to use the styled-components theme
export const useStyledTheme = (): DefaultTheme => useTheme();

// Export placeholder styled components (add actual styles later)
export const Container = styled.div`
  /* Container styles */
`;

export const ContentSection = styled.section`
  /* ContentSection styles */
`;

export const BlockContainer = styled.div`
  /* BlockContainer styles */
`;

export const BlockTitle = styled.h3`
  /* BlockTitle styles */
`;

export const BlockContent = styled.div`
  /* BlockContent styles */
`;

export const IconContainer = styled.div`
  /* IconContainer styles */
`; 